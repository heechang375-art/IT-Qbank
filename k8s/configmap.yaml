apiVersion: v1
kind: ConfigMap
metadata:
  name: quiz-config
  namespace: quiz-bank
data:
  DB_HOST:      "mysql-service"
  DB_PORT:      "3306"
  BACKEND_URL:  "http://backend-service:5000"
  FLASK_DEBUG:  "false"
