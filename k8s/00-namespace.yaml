# ──────────────────────────────────────────────────────────────────
# 00-namespace.yaml
# 모든 IT-Qbank 리소스가 배포되는 네임스페이스.
# kubectl apply -f k8s/00-namespace.yaml 을 가장 먼저 실행해야 함.
# (파일명 00- 접두사는 apply 순서상 가장 먼저 적용되도록 하기 위함)
# ──────────────────────────────────────────────────────────────────
apiVersion: v1
kind: Namespace
metadata:
  name: hc-quiz-bank
  labels:
    app: hc-quiz-bank
