apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: hc-quiz-bank
  labels:
    app: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: collapse/quiz-frontend:v0.1
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: BACKEND_URL
              valueFrom:
                configMapKeyRef:
                  name: frontend-config
                  key: BACKEND_URL
            - name: FLASK_DEBUG
              valueFrom:
                configMapKeyRef:
                  name: frontend-config
                  key: FLASK_DEBUG
            - name: FRONTEND_PROXY_TIMEOUT
              valueFrom:
                configMapKeyRef:
                  name: frontend-config
                  key: FRONTEND_PROXY_TIMEOUT
            - name: PORT
              value: "8080"
          resources:
            requests:
              memory: "64Mi"
              cpu: "40m"
            limits:
              memory: "128Mi"
              cpu: "100m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3


